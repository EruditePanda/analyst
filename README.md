#Devops experiments repo

##Terraform

add to your .zshrc file the following:
```
SCALEWAY_ORGANIZATION=access_key (from web UI, yes, it's strange, maybe they will fix it)
SCALEWAY_ACCESS_KEY=token (from web UI)
```
then use
```
terraform plan
```
and
```
terraform apply
```
